/** @format */

import { CONTACT_INFO } from './app';
import { APP_ANCHOR_TAG, EXTERNAL_URL } from './routes/url';
import { HEIGHT_HEADER } from './styles';

export const CONSTANT = {
    CONTACT_INFO,
    APP_ANCHOR_TAG,
    EXTERNAL_URL,
    HEIGHT_HEADER,
} as const;
